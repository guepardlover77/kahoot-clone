services:
  kahoot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - kahoot-data:/app/server/prisma
    restart: unless-stopped

volumes:
  kahoot-data:
